[package]
name = "verity-consensus"
description = "The Verity consensus client — single starting crate per the 2026-07-22 kickoff decision (CI canary stub — replace with the real implementation)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

# The pinned dependency set, declared here so it is actually resolved, compiled, and written to
# Cargo.lock — `[workspace.dependencies]` on its own locks nothing. Nothing consumes these yet;
# they are wired in so CI proves the set builds together before implementation depends on it.
[dependencies]
leansig.workspace = true
libssz.workspace = true
libssz-derive.workspace = true
libssz-merkle.workspace = true
libssz-types.workspace = true
libp2p.workspace = true
rocksdb.workspace = true

[lints]
workspace = true
